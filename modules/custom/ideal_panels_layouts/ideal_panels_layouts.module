<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function ideal_panels_layouts_ctools_plugin_directory($owner, $plugin_type) {
  if (($owner == 'panels' && $plugin_type == 'layouts')) {
    return "plugins/$plugin_type";
  }
}
