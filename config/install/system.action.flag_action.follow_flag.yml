langcode: en
status: true
dependencies:
  config:
    - flag.flag.follow
  module:
    - flag
id: flag_action.follow_flag
label: Follow
type: node
plugin: 'flag_action:follow_flag'
configuration:
  flag_id: follow
  flag_action: flag
