<?php
/**
 * @file
 * Code for the Ideas Projects Manager feature.
 */

include_once('ideas_projects_manager.features.inc');


/**
 * Implements hook_menu().
 */
function ideas_projects_manager_menu() {
  $items = array();
  $items['admin/config/user-interface/openideal/ideas_projects_manager'] = array(
    'title' => 'Ideas Projects Manager',
    'access arguments' => array('access content'),
    'page callback' => 'theme',
    'page arguments' => array('ideas_projects_manager_page'),  
    'file' => 'ideas_projects_manager.admin.inc',  
    'type' => MENU_LOCAL_TASK,
  );
  return $items;
}

/**
 * Implements hook_theme()
 */
function ideas_projects_manager_theme($existing, $type, $theme, $path) {
  return array(
    'ideas_projects_manager_page' => array(),
  );
}
